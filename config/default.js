const config = {
  // 啟動端口
  port: 3003,
  // 資料庫配置
  database: {
    DATABASE: 'nodesql',
    USERNAME: 'root',
    PASSWORD: '',
    PORT: '3306',
    HOST: 'localhost'
  }
}

module.exports = config